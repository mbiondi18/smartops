apiVersion: v1
kind: Service
metadata:
  name: smartops-frontend-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: smartops-frontend
  ports:
    - port: 80
      targetPort: 3000
