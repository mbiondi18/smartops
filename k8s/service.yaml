apiVersion: v1
kind: Service
metadata:
  name: smartops-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: smartops-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
